namespace ScaleCounter.Core;

/// <summary>Shared constants for exported preset files.</summary>
public static class PresetFile
{
    /// <summary>Custom file extension for exported presets (one or more per file).</summary>
    public const string Extension = ".uscpreset";

    /// <summary>Human-readable description for file dialogs.</summary>
    public const string Description = "USB Scale Counter preset";
}
